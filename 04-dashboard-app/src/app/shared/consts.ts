export const emailPattern = '^[a-z0-9._%+-]+@[a-z0-9.-]+\\.[a-z]{2,4}$';

export enum sweetAlertIcons {
  success = 'success',
  error = 'error',
  info = 'info',
  question = 'question',
  warning = 'warning'
}
